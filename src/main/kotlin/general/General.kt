package general

//import functional.NumberWithLogs
//import functional.addOneWithLogsBad
//import functional.squareWithLogsBad
import org.assertj.core.api.Assertions

fun general() {
    // TODO - I don't understand why this equals returns false
//    var a = addOneWithLogsBad(squareWithLogsBad(2))
//    var b = NumberWithLogs(5, listOf("Squared 2 to get 4, Added one to 4 to get 5"))
//    Assertions.assertThat(a).isEqualTo(b)

    /*
        TODO - Questions:
            1. null check needed for generics?
            2. Difference between (_: String) -> Int and (String) -> Int
     */
}